# Copy this file to .env and fill in your values
# Get your API key from: https://console.anthropic.com/api-keys

ANTHROPIC_API_KEY=your_api_key_here

# Optional: restrict which origins can call your API
# Leave blank to allow all origins (fine for personal use)
ALLOWED_ORIGIN=

# Port (Railway/Render set this automatically)
PORT=3000
